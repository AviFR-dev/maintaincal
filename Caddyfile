{
  # For LAN-only deployments without a public DNS name, you may want to use:
  # local_certs
}

{$MAINTAINCAL_HOST} {
  encode zstd gzip

  handle_path /api/* {
    reverse_proxy api:8080
  }

  handle {
    reverse_proxy web:80
  }
}

